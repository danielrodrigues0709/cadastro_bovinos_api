const { startServer, shutDownServer } = require(".");

/**
 * Rodar esse arquivo para iniciar servidor em dev
 * Não é utilizado em produção
 */
startServer();

// setTimeout(() => {
//     shutDownServer();
// }, 5000);